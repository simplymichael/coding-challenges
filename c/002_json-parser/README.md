## Running 
- Build by running `make` 
- Then run `./bin/json-parser ./tests/step<N>/<test_file>.json` 
  where <N> is the step number and <test_file> is one of the test files in the ./tests/step<N> folder.
